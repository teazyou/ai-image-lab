# fal.ai API key — copy this file to `.env` and paste your key.
#   cp .env.example .env   then edit the value below.
# Create / copy a key at: https://fal.ai/dashboard/keys
# `.env` is git-ignored (never committed); this .example is the tracked template.
FAL_KEY=
